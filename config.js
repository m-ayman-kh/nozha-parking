// Firebase project settings (public by design — access is controlled by firestore.rules).
// Set to null to run on the static copy in data/slots.geojson only.
window.NOZHA_CONFIG = {
  firebase: null,
};
